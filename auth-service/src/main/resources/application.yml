server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/authbd
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: my-very-secret-key-123456789
  expiration: 3600000